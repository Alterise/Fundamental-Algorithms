//#include <iostream>
#include "RB_tree.h"

template <typename T> void RB_tree<T>::insert(const int &key) {

}
template <typename T> RB_tree<T>& RB_tree<T>::search(const int &key) {
    RB_tree tree(this->_comparator);
    return tree;
}

template <typename T> void RB_tree<T>::remove(const int &key) {
}